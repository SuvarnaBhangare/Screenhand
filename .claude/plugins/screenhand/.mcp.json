{
  "mcpServers": {
    "screenhand": {
      "command": "npx",
      "args": ["tsx", "${SCREENHAND_PATH}/mcp-desktop.ts"],
      "env": {
        "ANTHROPIC_API_KEY": "${ANTHROPIC_API_KEY}"
      }
    }
  }
}
